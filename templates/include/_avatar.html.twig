{% if  %}
<img src ="{{ vich_uploader_asset(app.user.avatar) }}" alt="{{ app.user.firstName }} {{ app.user.lastName }}"
     title="{{ app.user.firstName }} {{ app.user.lastName }}" class="rounded-circle" height="50" width="50"/>
