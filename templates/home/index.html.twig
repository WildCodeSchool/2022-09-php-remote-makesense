{% extends 'base.html.twig' %}

{% block title %}Accueil - Makesense{% endblock %}

{% block body %}
    <h1>Bonjour {{ app.user.firstName }}</h1>

<div class = "home">
    <div class="container">
        <h2>Mes décisions à venir</h2>
    <div class="row justify-content-start custom-line">
    {% for userDecision in userDecisions %}
        <div class="card col-sm-10 col-md-5 col-lg-3 m-3">
                {% for timeline in userDecision.timelines %}
                    {% if loop.first %}
                        <div class="card-header bg-transparent border-dark">
                            Prochaine étape :
                            <br>{{ timeline.name }}
                            <br><i class="bi bi-calendar2-week-fill text-success me-2"></i><span>Le : {{ timeline.endedAt |date('d/m/Y') }}</span>
                        </div>
                    {% endif %}
                {% endfor %}
                <div class="card-body">
                    <h5 class="card-title text-primary">{{ userDecision.title }}</h5>
                </div>
                <div class="card-footer bg-transparent border-dark">
                    <img src ="https://picsum.photos/id/236/30/30" alt="{{ userDecision.user.firstName }} {{ userDecision.user.lastName }}" title="{{ userDecision.user.firstName }} {{ userDecision.user.lastName }}" class="rounded-circle"/> {{ userDecision.user.firstName }} {{ userDecision.user.lastName }}
                    <br><a href="{{ path('app_decision_show', {id: userDecision.id }) }}"
                           class="stretched-link"></a>
                </div>
        </div>

    {% else %}
        <div class="text-center">
            <img class="img-fluid" src="{{ asset('build/images/no-decision.png') }}">
            <p>Dommage...Il serait temps de créer un projet !</p>
            <button class="btn btn-primary rounded-3 text-white">Créer un nouveau projet</button>
        </div>

    {% endfor %}
    </div>

    <br><a href="{{ path('app_decision_mine') }}">Voir toutes mes décisions 💫</a>

    <h2 class="mt-5">Mes contributions à venir</h2>
    {% for userContribution in userContributions %}
        {% if userContribution.decision.user.id != app.user.id %}
        <div class="row d-inline-flex">
            <div class="card col-sm-9 mb-3 m-3" style="width: 18rem;">
                <div class="card-header bg-transparent border-dark">
                    {{ userContribution.name }}
                    <br><i class="bi bi-calendar2-week-fill text-success me-2"></i><span>Le : {{ userContribution.endedAt |date('d/m/Y') }}</span>
                </div>
                <div class="card-body text-success">
                    <h5 class="card-title">{{ userContribution.decision.title }}</h5>
                </div>
                <div class="card-footer bg-transparent border-dark">
                    <img src ="https://picsum.photos/id/236/30/30" alt="{{ userContribution.decision.user.firstName }} {{ userContribution.decision.user.lastName }}"
                         title="{{ userContribution.decision.user.firstName }} {{ userContribution.decision.user.lastName }}" class="rounded-circle"/>
                    {{ userContribution.decision.user.firstName }} {{ userContribution.decision.user.lastName }}
                    <br><a href="{{ path('app_decision_show', {id: userContribution.decision.id }) }}"
                           class="stretched-link"></a>
                </div>
            </div>
        </div>
        {% endif %}
    {% else %}
        <p>Aucune contribution en cours</p>
    {% endfor %}
    <br><a href="{{ path('app_contributor_decisions') }}">Voir toutes mes contributions ⭐️</a>
    </div>
</div>
{% endblock %}
