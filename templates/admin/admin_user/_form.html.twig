{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary m-2"><i class="bi bi-check-lg me-2"></i><span>{{ button_label|default('Enregistrer') }}</span></button>
{{ form_end(form) }}
