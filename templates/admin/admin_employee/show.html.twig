{% extends 'admin/base.html.twig' %}

{% block title %}Employee{% endblock %}

{% block body %}
    <h1>Employee</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ employee.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ employee.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ employee.lastname }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ employee.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_employee_index') }}">back to list</a>

    <a href="{{ path('app_admin_employee_edit', {'id': employee.id}) }}">edit</a>

    {{ include('admin/admin_employee/_delete_form.html.twig') }}
{% endblock %}
