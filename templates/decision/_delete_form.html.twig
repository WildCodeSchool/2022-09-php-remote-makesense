<form method="post" action="{{ path('app_decision_delete', {'id': decision.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette décision ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ decision.id) }}">
    <button class="btn btn-danger m-2"><i class="bi bi-trash3 me-2"></i><span>Supprimer</span></button>
</form>
